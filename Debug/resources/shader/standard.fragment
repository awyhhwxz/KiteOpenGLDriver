#version 400 core
precision mediump float;
uniform sampler2D tex;
in vec2 uv;
in vec3 normal;
out vec4 FragColor;
void main()		
{
	vec3 lightDir = vec3(1.0, -1.0, 1.0);
	float diffuse = dot(lightDir, normal) * 0.5 + 0.5;
	FragColor = texture2D(tex, uv) * diffuse;		
}